//! JIT compiler for Brainfuck based on LLVM.

use llvm_sys;

struct Compiler {
    
}

impl Compiler {
    fn new() {
        unsafe {
//            let int64 = LLVMInt64Type

            //        llvm_sys::core::LLVMFunctionType()
        }
    }
}

